<tool id="classviz" name="Classviz-Tool" version="1.0.0">
  <description>Visualize code architecture in JSON format</description>
  <command detect_errors="exit_code">
    python3 '${__tool_directory__}/pre_classviz.py' '$input' '${__tool_directory__}/data/input.json'; python3 -m http.server -b 127.0.0.42 7800 -d '${__tool_directory__}/'
  </command>
  <inputs>
    <param name="input" type="data" format="json" multiple="False" label="JSON file" help="JSON file to be visualized."/>
  </inputs>
  <outputs>
    <data format="txt" name="link" label="Go to http://127.0.0.42:7800/"/>
  </outputs>
  <help>
    This tool takes a JSON file as input and visualize the code architecture. Delete the job once done to kill the server.
  </help>
</tool>
